using System;
using System.Collections.Generic;
using System.Text;

namespace Rovers
{
    public enum Direction : byte
    {
        North = (byte)'N',
        West = (byte)'W',
        South = (byte)'S',
        East = (byte)'E'
    }
}
